#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
    std::vector<int> a;
    while (true) {
        a.resize(a.size() + 1e6);  // Keeps growing vector
    }
    return 0;
}